<div class="jumbotron">
    <% if current_user.list.present? %>
          <%= render partial: 'lists/list' %>
      <% elsif current_user.present? && @list.nil? %>
        <%= link_to "New To-do List", new_list_path, class: 'btn btn-success' %>
      <% else %>
        <%= render 'signup' %>
    <% end %>
</div>

  <div class="col-md-4", class='pull-right'>
  </div>
