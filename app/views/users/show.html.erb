<h1>User Profile</h1>

<% if @list.nil? %>
   <%= link_to "New To-do List", new_list_path, class: 'btn btn-success' %>
 <% end %>

 <% else %>

 <div class="row">
  <div class="col-md-8">
    <% @user.list%>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to current_user.list, list %>
          </h4>
          <small>
            <%= current_user.list.description %>
          </small>
        </div>
      </div>
    
    </div>
    </div>
