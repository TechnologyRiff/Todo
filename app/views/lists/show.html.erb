<% if @list.nil? %>
   <%= link_to "New To-do List", new_list_path, class: 'btn btn-success' %>
<% else %>

  <div class="row">
    <div class="col-md-8", class: 'pull-left'>
    <%= render partial: 'lists/list', locals: { list: @list, item: @item } %>
        
        
    </div>
  </div>
<% end %>
