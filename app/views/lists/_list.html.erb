      
       <%#= @list.description %>

    <div class="row">
        <span class="col-md-12"><h2>List: <%= @list.title %></h2></span>
        <%= link_to "Edit List", edit_list_path(:list_id), class: 'btn btn-success' %>
        <% if policy(@list).destroy? %>
          <%= link_to "Delete List", @list, method: :delete, class: 'btn btn-danger' %>
        <% end %>
    </div>
            <p>
              <% unless @list.items.nil? %>
                <div class="container">
                  <div class="row">
                    <div class="col-md-8 pull-left">
                  
                      <h3>Current tasks:</h3>
                        <!-- <div class="dropdown">
                          <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-expanded="true">
                            Sort by
                            <span class="caret"></span>
                          </button>
                          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdown">
                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Newest First</a></li>
                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Oldest First</a></li>
                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">Title</a></li>
                          </ul>
                        </div> -->
                      <div class='js-items' class="col-md-8 pull-left">
                        <%= render partial: 'items/item', collection: @items %>
                      </div>
              <% end %> 
                    <br />
                        <div class='new-item' class="col-md-8 pull-left">
                          <%= render partial: 'items/form', locals: { list: @list } %>
                        </div>
                  
                      </div> 
                    </div>
            </p>

